# Requirements for the Flask API
# These get installed inside the Docker container

flask==3.0.0           # Web framework — turns Python into a web server
scikit-learn==1.3.2    # ML library — needed to load the trained model
joblib==1.3.2          # Used to load/save model files (.pkl)
numpy==1.26.2          # Number crunching — needed by scikit-learn
